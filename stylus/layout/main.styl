
container()
	center: (app-width + (2 * app-gap))

.container
	container()

.section
	container()
	padding 0 10px
	box-sizing border-box

.gui
	min-height 100vh
	// overflow hidden
	display flex
	flex-direction column
	justify-content stretch
	
	// +below( $laptop )
		// overflow auto